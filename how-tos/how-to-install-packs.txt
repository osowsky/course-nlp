# Install spacy
pip install spacy --upgrade

# Install portuguese language module for spaCy.
python -m spacy download pt_core_news_sm

# Install this module to work with displacy on web.
python -m spacy download en_core_web_sm

# Install (numpy), and matplotlib
pip install matplotlib

# Install TensorFlow, Pandas, SeaBorn, and scikit-learn.
pip install tensorflow   (machine learning: tf.keras: deep learning API)
pip install pandas       (data analysis)
pip install seaborn      (statisstical data visualization)
pip install scikit-learn (machine learning)

# Istall wget to download the datasets.
# dont install wget, instead install python3-wget
pip install python3-wget

# Install gensim to work on word2vec.
pip install gensim
